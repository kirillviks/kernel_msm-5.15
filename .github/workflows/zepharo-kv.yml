name: "Build ZepharoKV"

on:
  workflow_dispatch:

env:
  WS_WORK_DIR: ${{ github.workspace }}
  WS_KERNEL_SOURCE: https://github.com/topnotchfreaks/kernel_msm-5.15
  WS_KERNEL_SOURCE_BRANCH: zepharo-new
  WS_KERNEL_LOCALVERSION: ZepharoKV
  WS_KERNEL_CONFIG_FILE: arch/arm64/configs/unified_defconfig

jobs:
  Build-Kernel:
    name: "🚀 Build Kernel"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        variant: [ksu, noksu]

    steps:
      - name: "✨ Setup workspace"
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y repo rsync aria2

      - name: 📐 Setup SWAP
        run: |
          sudo fallocate -l 8G /tempswapfile
          sudo chmod 600 /tempswapfile
          sudo mkswap /tempswapfile
          sudo swapon /tempswapfile
          echo "vm.swappiness=60" | sudo tee -a /etc/sysctl.conf
          sudo sysctl -p

      - name: "🌟 Clone kernel source"
        working-directory: ${{ env.WS_WORK_DIR }}
        run: |
          git clone ${{ env.WS_KERNEL_SOURCE }} ${{ env.WS_KERNEL_LOCALVERSION }} --recursive --branch ${{ env.WS_KERNEL_SOURCE_BRANCH }} --depth=1

      - name: "📥 Initialize repo"
        working-directory: ${{ env.WS_WORK_DIR }}
        run: |
          repo init -u https://android.googlesource.com/kernel/manifest -b common-android13-5.15-lts --depth=1
          repo sync --optimized-fetch --prune --no-clone-bundle --no-tags -j$(nproc --all)

          aria2c -x 16 -s 16 -o prebuilts/clang/host/linux-x86/clang-r547379.tar.gz "https://github.com/topnotchfreaks/clang/releases/download/v1.0.0/clang-r547379.tar.gz"
          mkdir -p prebuilts/clang/host/linux-x86/clang-r547379
          tar -xzf prebuilts/clang/host/linux-x86/clang-r547379.tar.gz -C prebuilts/clang/host/linux-x86/clang-r547379
          rm -rf prebuilts/clang/host/linux-x86/clang-r547379.tar.gz

          rm -rf prebuilts/clang/host/linux-x86/clang-3289846
          rm -rf prebuilts/clang/host/linux-x86/clang-r450784e
          rm -rf prebuilts/clang/host/linux-x86/clang-stable

          sed -i \
           -e 's/^BRANCH=.*/BRANCH=android13-5.15/' \
           -e 's/^CLANG_VERSION=.*/CLANG_VERSION=r547379/' \
           ${{ env.WS_KERNEL_LOCALVERSION }}/build.config.constants

          sed -i 's/^DEFCONFIG=.*/DEFCONFIG=unified_defconfig/' ${{ env.WS_KERNEL_LOCALVERSION }}/build.config.gki
          sed -i '/^POST_DEFCONFIG_CMDS/d' ${{ env.WS_KERNEL_LOCALVERSION }}/build.config.gki

          sed -i \
           -e '/^KMI_SYMBOL_LIST_STRICT_MODE=/d' \
           -e '/^TRIM_NONLISTED_KMI=/d' \
           -e '/^KMI_ENFORCED=/d' \
           -e '$a\KMI_SYMBOL_LIST_STRICT_MODE=0' \
           -e '$a\TRIM_NONLISTED_KMI=0' \
           -e '$a\KMI_ENFORCED=0' \
           -e '/^MODULES_ORDER=/d' \
           -e '/^MODULES_LIST=/d' \
           ${{ env.WS_KERNEL_LOCALVERSION }}/build.config.gki.aarch64

      - name: "⚙️ Configure kernel"
        working-directory: ${{ env.WS_KERNEL_LOCALVERSION }}
        run: |
          # Set localversion
          if [ -n "${{ env.WS_KERNEL_LOCALVERSION }}" ]; then
            echo "CONFIG_LOCALVERSION=\"${{ env.WS_KERNEL_LOCALVERSION }}-$(date +'%Y-%m-%d')\"" >> ${{ env.WS_KERNEL_CONFIG_FILE }}
            echo "CONFIG_LOCALVERSION_AUTO=n" >> ${{ env.WS_KERNEL_CONFIG_FILE }}
          fi

          # Remove existing optimization configs and apply O3 optimization level
          sed -i '/CONFIG_CC_OPTIMIZE/d' ${{ env.WS_KERNEL_CONFIG_FILE }}
          echo "CONFIG_CC_OPTIMIZE_FOR_PERFORMANCE_O3=y" >> ${{ env.WS_KERNEL_CONFIG_FILE }}

          cat >> ${{ env.WS_KERNEL_CONFIG_FILE }} << 'EOF'
          CONFIG_KALLSYMS=y
          CONFIG_TMPFS_XATTR=y
          CONFIG_TMPFS_POSIX_ACL=y
          EOF

      - name: "🔧 Setup Wild KSU with SUSFS"
        if: matrix.variant == 'ksu'
        working-directory: ${{ env.WS_KERNEL_LOCALVERSION }}
        run: |
          rm -rf ./KernelSU ./drivers/kernelsu

          echo "📦 Installing Wild KSU..."

          # Install WildKSU with SUSFS
          curl -LSs "https://raw.githubusercontent.com/WildKernels/Wild_KSU/wild/kernel/setup.sh" | bash -s stable

          echo "✔ Wild KSU installed successfully"

          # Clone SUSFS4KSU for kernel patches only (not KSU integration)
          echo "📦 Cloning SUSFS4KSU for kernel patches..."
          git clone --depth=1 https://gitlab.com/simonpunk/susfs4ksu.git -b gki-android13-5.15

          SUSFS_KERNEL_PATCH="susfs4ksu/kernel_patches/50_add_susfs_in_gki-android13-5.15.patch"

          echo "🔧 Applying SUSFS kernel patch..."
          if patch -p1 < "$SUSFS_KERNEL_PATCH"; then
            echo "✅ SUSFS patch successfully applied"
          else
            echo "⚠️ SUSFS patch failed — copying files manually"
          fi

          echo "🔧 Applying SUSFS -> KernelSU patch..."
          KSU_PATCH="susfs4ksu/kernel_patches/KernelSU/10_enable_susfs_for_ksu.patch"

          echo "📄 Forcing SUSFS source injection..."

          # Always replace fs/susfs.c
          mkdir -p fs
          cp -f susfs4ksu/kernel_patches/fs/susfs.c fs/susfs.c
          echo "✔ Copied fs/susfs.c"

          # Always replace include/linux/susfs.h
          mkdir -p include/linux
          cp -f susfs4ksu/kernel_patches/include/linux/susfs.h include/linux/susfs.h
          echo "✔ Copied include/linux/susfs.h"

          # Also install missing susfs_def.h
          if [ -f susfs4ksu/kernel_patches/include/linux/susfs_def.h ]; then
            cp -f susfs4ksu/kernel_patches/include/linux/susfs_def.h include/linux/susfs_def.h
            echo "✔ Copied include/linux/susfs_def.h"
          else
            echo "⚠️ susfs_def.h not found in repo"
          fi

          echo "🔍 Verifying SUSFS files..."
          for f in fs/susfs.c include/linux/susfs.h include/linux/susfs_def.h; do
            if [ -f "$f" ]; then echo "✔ Found: $f"; else echo "❌ Missing: $f"; fi
          done

          # Clean up SUSFS repo (we only needed kernel patches, not KSU integration)
          rm -rf susfs4ksu

          # Create symbolic link for drivers/kernelsu (if needed by build system)
          if [ ! -L "drivers/kernelsu" ]; then
            ln -sf ../KernelSU drivers/kernelsu
            echo "✔ Created drivers/kernelsu symlink"
          fi

          # Add Wild KSU configs with SUSFS
          cat >> ${{ env.WS_KERNEL_CONFIG_FILE }} << 'EOF'
          CONFIG_KSU=y
          CONFIG_KPROBES=y
          CONFIG_HAVE_KPROBES=y
          CONFIG_KPROBE_EVENTS=y
          CONFIG_OVERLAY_FS=y
          CONFIG_KSU_SUSFS=y
          CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_PATH=y
          CONFIG_KSU_SUSFS_SUS_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SUS_KSTAT=y
          CONFIG_KSU_SUSFS_TRY_UMOUNT=y
          CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT=y
          CONFIG_KSU_SUSFS_SPOOF_UNAME=y
          CONFIG_KSU_SUSFS_ENABLE_LOG=y
          CONFIG_KSU_SUSFS_HIDE_KSU_SUSFS_SYMBOLS=y
          CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG=y
          CONFIG_KSU_SUSFS_OPEN_REDIRECT=y
          CONFIG_KSU_SUSFS_SUS_SU=y
          EOF

          echo "🎉 Wild KSU with SUSFS kernel patches fully integrated!"

      - name: "🔧 Setup Baseband-guard"
        if: matrix.variant == 'ksu'
        working-directory: ${{ env.WS_KERNEL_LOCALVERSION }}
        run: |
          curl -LSs "https://raw.githubusercontent.com/vc-teahouse/Baseband-guard/main/setup.sh" | bash
          echo "CONFIG_BBG=y" >> ${{ env.WS_KERNEL_CONFIG_FILE }}
          sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig

      - name: "🔨 Build kernel"
        working-directory: ${{ env.WS_WORK_DIR }}
        run: |
          LTO=full BUILD_CONFIG=${{ env.WS_KERNEL_LOCALVERSION }}/build.config.gki.aarch64 DIST_DIR=dist build/build.sh -j$(nproc --all)

      - name: "📦 Prepare artifact"
        working-directory: ${{ env.WS_WORK_DIR }}
        run: |
          mv dist/Image Image.${{ matrix.variant }}

      - name: "📤 Upload artifact"
        uses: actions/upload-artifact@v4
        with:
          name: kernel-${{ matrix.variant }}
          path: ${{ env.WS_WORK_DIR }}/Image.${{ matrix.variant }}
          retention-days: 3

  Package-Kernels:
    name: "📦 Package kernels"
    runs-on: ubuntu-latest
    needs: Build-Kernel
    steps:
      - name: "📥 Download artifacts"
        uses: actions/download-artifact@v4
        with:
          path: kernels

      - name: "📦 Create flashable ZIP"
        run: |
          WS_ANYKERNEL3_NAME=AnyKernel3-${{ env.WS_KERNEL_LOCALVERSION }}-$(date +'%Y-%m-%d')
          echo "WS_ANYKERNEL3_NAME=$WS_ANYKERNEL3_NAME" >> $GITHUB_ENV

          git clone --depth=1 https://github.com/topnotchfreaks/AnyKernel3 -b master AnyKernel3

          # Copy kernels
          find kernels -name "Image.*" -exec cp {} AnyKernel3 \;

          # Create ZIP
          cd AnyKernel3
          rm -rf .git
          zip -r9 "../$WS_ANYKERNEL3_NAME.zip" *
          cd ..
          echo "WS_ANYKERNEL3_ZIP_PATH=$(pwd)/$WS_ANYKERNEL3_NAME.zip" >> $GITHUB_ENV

      - name: "📤 Upload final package"
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.WS_ANYKERNEL3_NAME }}
          path: AnyKernel3/*
          compression-level: 9
          retention-days: 7

      - name: "📱 Send to Telegram"
        if: always()
        run: |
          if [[ -f "${{ env.WS_ANYKERNEL3_ZIP_PATH }}" ]]; then
            curl -s -F document=@"${{ env.WS_ANYKERNEL3_ZIP_PATH }}" \
                 -F caption="✅ Kernel Build Complete - $(date '+%Y-%m-%d %H:%M:%S')" \
                 -F chat_id=${{ secrets.TELEGRAM_USER_ID }} \
                 "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument"
          else
            curl -s -X POST \
                 -d chat_id=${{ secrets.TELEGRAM_USER_ID }} \
                 -d text="❌ Kernel build failed" \
                 "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage"
          fi
